import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App"; // Переконайтеся, що файл App.jsx або App.tsx існує

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
